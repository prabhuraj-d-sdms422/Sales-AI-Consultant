"""
PHASE 2 PLACEHOLDER — No code here in V1.

When Phase 2 begins:
1. pip install asyncpg sqlalchemy[asyncio] alembic psycopg2-binary
2. Uncomment DB host vars and DATABASE_URL in .env
3. Uncomment database_url in settings.py
4. Implement async engine + async_session factory here
5. Run: alembic init migrations && alembic revision --autogenerate && alembic upgrade head
"""
